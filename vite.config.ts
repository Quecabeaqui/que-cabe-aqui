import { defineConfig } from 'vite';

export default defineConfig({
  // Relative asset paths keep the app working on both the GitHub Pages
  // project URL and a future custom domain.
  base: './',
});
